document.addEventListener('DOMContentLoaded', () => {
    console.log('Página do Chatbot carregada.');

    // No futuro, aqui entrará a lógica para:
    // 1. Clicar em "Conectar" e abrir um pop-up de autenticação.
    // 2. Mudar o status do card para "Conectado".
    // 3. Carregar as configurações da IA.
});