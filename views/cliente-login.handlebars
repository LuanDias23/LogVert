{{!--
  View de Login do Cliente usando Número Serial
  O cliente usa o serial gerado na venda para acessar suas informações
--}}

<div class="login-container">
    <div class="login-particles" id="loginParticles"></div>

    <div class="login-card">
        <div class="login-header">
            <img src="/assets/logosemfundo.png" alt="LogVert Logo" class="login-logo">
            <h1>Área do Cliente</h1>
            <p>Acesse suas compras usando o número serial</p>
        </div>

        <form id="clienteLoginForm" class="login-form">
            <div class="form-group">
                <label for="numeroSerial">
                    <i class="fas fa-key"></i> Número Serial
                </label>
                <input
                    type="text"
                    id="numeroSerial"
                    name="numeroSerial"
                    placeholder="ABC1234-5678-9012"
                    required
                    maxlength="17"
                    pattern="[A-Z]{3}[0-9]{4}-[0-9]{4}-[0-9]{4}"
                    title="Formato: ABC1234-5678-9012"
                    autocomplete="off"
                >
                <small class="input-hint">
                    <i class="fas fa-info-circle"></i>
                    Digite o número serial recebido por email
                </small>
            </div>

            <button type="submit" class="btn-login">
                <i class="fas fa-sign-in-alt"></i> Acessar Minhas Compras
            </button>

            <div id="loginMessage" class="login-message"></div>
        </form>

        <div class="login-footer">
            <p><i class="fas fa-envelope"></i> Não recebeu o serial?</p>
            <a href="/login" class="link-secondary">
                Entre em contato com a loja
            </a>
        </div>

        <div class="login-info">
            <h3><i class="fas fa-shield-alt"></i> Como funciona?</h3>
            <ul>
                <li><i class="fas fa-check"></i> Ao realizar uma compra, você recebe um número serial por email</li>
                <li><i class="fas fa-check"></i> Use este número para acompanhar seu pedido</li>
                <li><i class="fas fa-check"></i> Você pode solicitar trocas e devoluções</li>
                <li><i class="fas fa-check"></i> Seus dados ficam seguros e protegidos</li>
            </ul>
        </div>
    </div>
</div>

<style>
    /* Reset básico para esta página */
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Roboto', sans-serif;
        background: linear-gradient(135deg, #0a1929 0%, #1a2332 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #e8eaed;
        overflow-x: hidden;
    }

    .login-container {
        position: relative;
        width: 100%;
        max-width: 500px;
        padding: 2rem;
    }

    .login-particles {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
        pointer-events: none;
    }

    .login-card {
        position: relative;
        z-index: 1;
        background: rgba(26, 35, 50, 0.85);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 16px;
        padding: 2.5rem;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
    }

    .login-header {
        text-align: center;
        margin-bottom: 2rem;
    }

    .login-logo {
        width: 80px;
        height: auto;
        margin-bottom: 1rem;
    }

    .login-header h1 {
        font-size: 1.8rem;
        margin-bottom: 0.5rem;
        color: #4CAF50;
    }

    .login-header p {
        color: #9aa0a6;
        font-size: 0.95rem;
    }

    .login-form {
        margin-bottom: 2rem;
    }

    .form-group {
        margin-bottom: 1.5rem;
    }

    .form-group label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: 500;
        color: #9aa0a6;
        font-size: 0.95rem;
    }

    .form-group label i {
        margin-right: 0.5rem;
        color: #4CAF50;
    }

    .form-group input {
        width: 100%;
        padding: 14px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 10px;
        background: rgba(10, 25, 41, 0.6);
        color: #e8eaed;
        font-size: 1rem;
        font-family: 'Courier New', monospace;
        letter-spacing: 1px;
        transition: all 0.3s ease;
    }

    .form-group input:focus {
        outline: none;
        border-color: #4CAF50;
        box-shadow: 0 0 0 3px rgba(76, 175, 80, 0.2);
        background: rgba(10, 25, 41, 0.8);
    }

    .form-group input::placeholder {
        color: #5f6368;
        letter-spacing: 0.5px;
    }

    .input-hint {
        display: block;
        margin-top: 0.5rem;
        font-size: 0.85rem;
        color: #9aa0a6;
    }

    .input-hint i {
        margin-right: 0.25rem;
    }

    .btn-login {
        width: 100%;
        padding: 16px;
        border: none;
        border-radius: 10px;
        background: linear-gradient(135deg, #4CAF50 0%, #388E3C 100%);
        color: white;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(76, 175, 80, 0.3);
    }

    .btn-login:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(76, 175, 80, 0.4);
    }

    .btn-login:active {
        transform: translateY(0);
    }

    .btn-login i {
        margin-right: 0.5rem;
    }

    .login-message {
        margin-top: 1rem;
        padding: 1rem;
        border-radius: 8px;
        text-align: center;
        font-size: 0.9rem;
        display: none;
    }

    .login-message.show {
        display: block;
    }

    .login-message.loading {
        background: rgba(26, 115, 232, 0.15);
        color: #1a73e8;
        border: 1px solid #1a73e8;
    }

    .login-message.success {
        background: rgba(76, 175, 80, 0.15);
        color: #4CAF50;
        border: 1px solid #4CAF50;
    }

    .login-message.error {
        background: rgba(229, 57, 53, 0.15);
        color: #e53935;
        border: 1px solid #e53935;
    }

    .login-footer {
        text-align: center;
        padding-top: 1.5rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
    }

    .login-footer p {
        color: #9aa0a6;
        font-size: 0.9rem;
        margin-bottom: 0.5rem;
    }

    .link-secondary {
        color: #1a73e8;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.2s;
    }

    .link-secondary:hover {
        color: #4CAF50;
        text-decoration: underline;
    }

    .login-info {
        margin-top: 2rem;
        padding: 1.5rem;
        background: rgba(76, 175, 80, 0.05);
        border-left: 4px solid #4CAF50;
        border-radius: 8px;
    }

    .login-info h3 {
        font-size: 1rem;
        margin-bottom: 1rem;
        color: #4CAF50;
    }

    .login-info h3 i {
        margin-right: 0.5rem;
    }

    .login-info ul {
        list-style: none;
        padding: 0;
    }

    .login-info li {
        padding: 0.5rem 0;
        color: #9aa0a6;
        font-size: 0.9rem;
        line-height: 1.5;
    }

    .login-info li i {
        margin-right: 0.5rem;
        color: #4CAF50;
    }

    @media (max-width: 600px) {
        .login-container {
            padding: 1rem;
        }

        .login-card {
            padding: 1.5rem;
        }

        .login-header h1 {
            font-size: 1.5rem;
        }
    }
</style>
